name: Publish RTB.Theme

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/RTB.Theme/**'
      - '.github/workflows/nuget-publish-theme.yml'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Add GitHub Packages source
        run: dotnet nuget add source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" --name github --username ${{ github.repository_owner }} --password ${{ secrets.GH_NUGET_TOKEN }} --store-password-in-clear-text

      - name: Restore dependencies
        run: dotnet restore src/RTB.Theme/RTB.Theme.csproj

      - name: Build
        run: dotnet build src/RTB.Theme/RTB.Theme.csproj --configuration Release

      - name: Pack
        run: dotnet pack src/RTB.Theme/RTB.Theme.csproj --configuration Release

      - name: Publish to GitHub Packages
        run: dotnet nuget push "src/RTB.Theme/bin/Release/*.nupkg" --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" --api-key ${{ secrets.GH_NUGET_TOKEN }} --skip-duplicate